<ul class="dropdown-menu">
    <li class="list-group">@Html.ActionLink("Tất cả sản phẩm", "Index","Home")</li>
    @foreach(var item in ViewBag.LoaiSP)
    {
        <li class="list-group">
            <a href="@Url.Action("Index","Home", new { MaLoaiSP = item.Key})">
            @item.Value</a>
        </li>
    }
    
    
</ul>
